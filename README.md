# 📋 Telegram Daily Report Bot

Telegram бот для автоматизации ежедневной отчётности call-центра с интеграцией Google Sheets и Mini App формой.

## 🎯 Описание проекта

Бот позволяет сотрудникам быстро отправлять ежедневные отчёты через удобную форму в Telegram, автоматически сохраняя данные в Google Таблице руководителя.

### 🌟 Основные возможности

- ✅ **Регистрация сотрудников** через команду `/start`
- 📊 **Удобная форма отчёта** как Telegram Mini App
- 📈 **Автоматическое сохранение** в Google Sheets
- ⏰ **Напоминания** о необходимости отправить отчёт
- 👨‍💼 **Админ-панель** для руководителя
- 📱 **Мобильный интерфейс** оптимизирован для телефонов

## 🏗️ Технологический стек

- **Backend:** Python 3.11+ + aiogram 3.x
- **Frontend:** HTML5/CSS3/JavaScript (Mini App)
- **Database:** SQLite
- **API:** Google Sheets API, Telegram Bot API
- **Scheduler:** APScheduler
- **Hosting:** Render.com (бесплатно)

## 📦 Установка и настройка

### 1. Клонирование проекта

```bash
git clone https://github.com/your-username/telegram-report-bot.git
cd telegram-report-bot
```

### 2. Установка зависимостей

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\\Scripts\\activate
# или (Linux/Mac)
source venv/bin/activate

# Установить пакеты
pip install -r requirements.txt
```

### 3. Создание Telegram бота

1. Найти @BotFather в Telegram
2. Отправить команду `/newbot`
3. Указать имя: `Daily Report Bot`
4. Указать username: `your_company_report_bot`
5. Получить токен BOT_TOKEN

### 4. Настройка Google Sheets

#### Создание проекта в Google Cloud

1. Перейти на https://console.cloud.google.com
2. Создать новый проект или выбрать существующий
3. Включить Google Sheets API:
   - API & Services → Enable APIs and services
   - Найти "Google Sheets API" → Enable

#### Создание Service Account

1. IAM & Admin → Service Accounts
2. Create Service Account:
   - Name: `telegram-bot-service`
   - Description: `Service account for Telegram bot`
3. Выбрать роль: `Editor`
4. Create Key → JSON → Download

#### Создание Google Таблицы

1. Создать новую таблицу на https://sheets.google.com
2. Назвать лист: `Ежедневные отчёты`
3. Добавить заголовки в первую строку:
   ```
   A1: Дата
   B1: Сотрудник
   C1: Кол-во звонков
   D1: КП+
   E1: КП
   F1: Отказы
   G1: Неадекв
   H1: Время отправки
   ```
4. Скопировать ID таблицы из URL (между `/d/` и `/edit`)
5. Поделиться таблицей с email Service Account (из JSON файла)

### 5. Настройка переменных окружения

Создать файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполнить переменные:

```env
# Telegram Bot Configuration
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
ADMIN_TELEGRAM_ID=123456789

# Google Sheets Configuration
GOOGLE_SPREADSHEET_ID=1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms
GOOGLE_CREDENTIALS_PATH=credentials/google_credentials.json

# Application Settings
TIMEZONE=Europe/Moscow
REMINDER_TIME=18:00
REMINDER_REPEAT_AFTER_MINUTES=30
DATABASE_PATH=database/database.db

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/bot.log

# Development
DEBUG=True
```

### 6. Размещение credentials

Сохранить скачанный JSON файл как:
```
credentials/google_credentials.json
```

### 7. Инициализация базы данных

```bash
python -c "from services.database import DatabaseService; import asyncio; asyncio.run(DatabaseService('database/database.db').initialize())"
```

### 8. Запуск бота

```bash
python bot/main.py
```

## 🚀 Деплой на Render.com

### 1. Подготовка к деплою

```bash
git add .
git commit -m "Initial bot implementation"
git push origin main
```

### 2. Создание сервиса

1. Зарегистрироваться на https://render.com
2. New → Web Service
3. Подключить GitHub репозиторий
4. Настроить:
   - **Name:** telegram-report-bot
   - **Region:** Frankfurt (EU Central)
   - **Branch:** main
   - **Runtime:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot/main.py`
   - **Plan:** Free

### 3. Настройка переменных

В Render.com Dashboard → Environment добавить все переменные из `.env`

**Важно:** Для Google credentials создать переменную:
- `GOOGLE_CREDENTIALS_JSON` = содержимое JSON файла (как строка)

### 4. Деплой

Render автоматически запустит деплой. Проверить логи и протестировать бота.

## 📚 Использование

### Для сотрудников

1. **Регистрация:**
   ```
   /start → Введите ФИО → Готово!
   ```

2. **Отправка отчёта:**
   ```
   📊 Отправить отчёт → Заполнить форму → Отправить
   ```

3. **Проверка статуса:**
   ```
   📈 Мой статус → Просмотр отчётов
   ```

### Для руководителя

1. **Админ-панель:**
   ```
   /admin → 📊 Статус отчётов сегодня
   ```

2. **Просмотр сотрудников:**
   ```
   /admin → 👥 Список сотрудников
   ```

3. **Просмотр в Google Sheets:**
   - Открыть таблицу
   - Данные обновляются в реальном времени

## 🔧 Команды бота

| Команда | Описание | Доступ |
|---------|----------|---------|
| `/start` | Регистрация нового сотрудника | Все |
| `/help` | Справка по использованию | Все |
| `/status` | Статус отчётов пользователя | Сотрудники |
| `/admin` | Админ-панель | Только админ |

## 🗄️ Структура базы данных

### Таблица users
- `id` - ID пользователя
- `telegram_id` - Telegram ID
- `full_name` - ФИО сотрудника
- `username` - Telegram username
- `is_admin` - Администратор (да/нет)
- `is_active` - Активный (да/нет)

### Таблица reports
- `id` - ID отчёта
- `user_id` - ID пользователя
- `report_date` - Дата отчёта
- `calls_count` - Количество звонков
- `kp_plus` - Закрытые сделки
- `kp` - В работе
- `rejections` - Отказы
- `inadequate` - Неадекватные
- `submitted_at` - Время отправки

## 🐛 Решение проблем

### Бот не отвечает
1. Проверить BOT_TOKEN
2. Проверить, что бот запущен
3. Проверить логи: `tail -f logs/bot.log`

### Google Sheets не обновляется
1. Проверить GOOGLE_SPREADSHEET_ID
2. Проверить права доступа Service Account
3. Проверить credentials JSON

### Mini App не открывается
1. Проверить WEBAPP_URL
2. Проверить, что файлы webapp/ доступны
3. Тестировать в мобильном Telegram

### Напоминания не приходят
1. Проверить REMINDER_TIME и TIMEZONE
2. Проверить, что scheduler запущен
3. Проверить логи планировщика

## 📝 Логирование

Логи сохраняются в `logs/bot.log` и выводятся в консоль.

Уровни логирования:
- `DEBUG` - Подробная отладочная информация
- `INFO` - Основные события (по умолчанию)
- `WARNING` - Предупреждения
- `ERROR` - Ошибки
- `CRITICAL` - Критические ошибки

## 🔒 Безопасность

- ✅ Токены и ключи в переменных окружения
- ✅ `.env` файлы исключены из git
- ✅ Доступ к админ-функциям только для админов
- ✅ Валидация всех входящих данных
- ✅ Логирование подозрительной активности

## 📈 Мониторинг

### Ключевые метрики
- Время отклика бота: <1 секунды
- Uptime: >99%
- Процент ошибок: <1%
- Завершенность отчётов: >95%

### Проверки
- Ежедневно: статус бота и отчёты
- Еженедельно: анализ логов
- Ежемесячно: обновление зависимостей

## 🤝 Поддержка

При возникновении проблем:

1. Проверить FAQ выше
2. Просмотреть логи бота
3. Проверить статус Render.com
4. Обратиться к разработчику

## 📄 Лицензия

MIT License - свободное использование и модификация.

## 🔄 Changelog

### v1.0.0 (Текущая версия)
- ✅ Базовая функциональность
- ✅ Mini App форма
- ✅ Google Sheets интеграция
- ✅ Напоминания
- ✅ Админ-панель

### Планируемые обновления
- 📊 Статистика и аналитика
- 📧 Email уведомления
- 🔄 Редактирование отчётов
- 📱 Push-уведомления